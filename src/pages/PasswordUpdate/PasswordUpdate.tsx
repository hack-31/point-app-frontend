import { Box } from "@mui/material";

import { PasswordUpdate } from "@/features/account";

/**
 * path: /password-update
 */
export const PasswordUpdatePage = () => {
  return (
    <Box mx="10px">
      <PasswordUpdate />
    </Box>
  );
};
